extends layout
block enter
  .container
    .form-block
      .title-form-block Вход
      .form-form-block
        form(method="post" action="/enter-site")
          .form-group
            label(for="login") Логин:
            input(type="text" name="login" id="login" placeholder="dex1cre").form-control
          .form-group
            label(for="password") Пароль:
            input(type="password" name="password" id="password" placeholder="hellobitches12!%=D").form-control
          button.btn.btn-primary(style="margin: 0 auto; width: 150px; display: block;") Войти!
  style.
    .top-menu-container {
      display: none;
    }
